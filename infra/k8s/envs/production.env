NODE_ENV=production
PORT=4000
NEXULEX_API_BASE_URL=https://api.nexulex.com
POSTGRES_USER=causas
POSTGRES_DB=causas
# Only apps/admin is exposed publicly (apps/api is called server-side, over
# the in-cluster Service DNS — see apps/admin/src/lib/api-client.ts). One
# hostname, one ingress rule; see scripts/deploy/configure-cloudflare-tunnel.sh.
CLOUDFLARE_ADMIN_HOSTNAME=causas.liquidadoraconcursal.cl
